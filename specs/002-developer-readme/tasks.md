# Tasks: Developer Onboarding & README

**Input**: Design documents from `specs/002-developer-readme/`
**Prerequisites**: plan.md ✅, spec.md ✅, research.md ✅, quickstart.md ✅, data-model.md ✅

**Note**: This is a documentation-only feature. No Go code is produced. TDD (Principle II) is N/A. Tasks are pure authoring tasks — write and validate Markdown.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (independent files or independent sections)
- **[Story]**: User story this task belongs to (US1, US2, US3)

---

## Phase 1: Setup

**Purpose**: Create the file skeleton and assets directory so all authoring tasks have a target.

- [x] T001 Create `README.md` at repository root with skeleton section headers (no content yet)
- [x] T00X [P] Create `assets/` directory at repository root for optional logo SVG

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Produce the ASCII art logo and badges line — these appear at the top of the README and every subsequent section depends on having the header defined first.

**⚠️ CRITICAL**: US1, US2, and US3 authoring cannot begin until the README header identity is established.

- [x] T00X Write the ASCII art logo and tagline header in `README.md` — use the block-letter style from `quickstart.md`; verify it renders legibly in a terminal via `cat README.md`
- [x] T00X Add the three badges line (Go version, build status placeholder, license) immediately below the logo in `README.md`

**Checkpoint**: Foundation ready — `README.md` has a logo, tagline, and badges. All story authoring can now begin.

---

## Phase 3: User Story 1 — First-Time Developer Discovery (Priority: P1) 🎯 MVP

**Goal**: A developer who lands on the repository can understand what `cs` does and feel invited to try it — purely from the above-the-fold README content.

**Independent Test**: Share the README (sections through the demo block) with a developer unfamiliar with the project. They should be able to explain: "cs lets me run multiple Claude sessions with names I choose; no tmux needed." Success if they also express interest in installing it.

- [x] T00X [US1] Write the 2–3 sentence value proposition paragraph in `README.md` — emphasise human-owned session naming and multi-session scaling; use the copy from `quickstart.md`
- [x] T00X [US1] Write the static terminal demo block in `README.md` showing the fzf picker with `[ + new session ]` pre-selected and three example sessions below it; use the demo copy from `quickstart.md`

**Checkpoint**: US1 complete — the README hero section (logo + tagline + value prop + demo) is fully written and conveys the core identity.

---

## Phase 4: User Story 2 — Quick Install & First Run (Priority: P2)

**Goal**: A developer who has decided to try `cs` can go from zero to their first session in under 5 minutes following only the README.

**Independent Test**: Follow only the Quick-Start section from a clean macOS machine with Homebrew. Measure time-to-first-session. Should be ≤5 minutes.

- [x] T00X [US2] Write the "Prerequisites" section in `README.md` listing tmux 3.0+, fzf 0.35+, macOS, Go 1.22+ (build only), and the `brew install tmux fzf` one-liner; use content from `quickstart.md`
- [x] T00X [US2] Write the "Install" subsection in `README.md` with the build-from-source steps (`git clone`, `make build`, `sudo mv cs /usr/local/bin/`); note Homebrew tap as coming soon
- [x] T00X [US2] Write the "First run" subsection in `README.md` with the two-step sequence: `cs setup` then `cs`

**Checkpoint**: US2 complete — a macOS developer can follow the README and run `cs` for the first time.

---

## Phase 5: User Story 3 — Understanding the Mental Model (Priority: P3)

**Goal**: A developer who has installed the tool reads the README and understands that session names are human-chosen themes, not auto-generated by Claude.

**Independent Test**: After reading the README, ask a developer "who names the sessions in cs?" — they should answer "me/the developer", not "Claude".

- [x] T01X [US3] Write the "How it works" concept section in `README.md` — cover: you name your sessions, each name is a cognitive context, multiple parallel sessions are supported, cs uses an isolated tmux socket; use the copy and examples from `quickstart.md`
- [x] T01X [US3] Add the four concrete session name examples inside the "How it works" section: `auth-redesign`, `write-release-notes`, `debug-api-timeout`, `explore-graphql` — include a one-line description of each to show diversity of context

**Checkpoint**: US3 complete — the mental model section clearly communicates human ownership of session naming.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Add the usage reference, contributing section, and platform notes; then validate the full README against all spec acceptance criteria.

- [x] T01X [P] Write the "Usage" reference table in `README.md` covering: `cs`, `cs setup`, `cs list`, `cs list --json`, `cs --version`; use the table from `quickstart.md`
- [x] T01X [P] Write the "Contributing" paragraph in `README.md` — one paragraph, mention issues welcome, open an issue before large changes
- [x] T01X Add a "Platform support" note clarifying macOS primary, Linux planned, Windows not supported (tmux limitation)
- [x] T01X Proof-read the full `README.md` for consistency: verify all section headings are correct, all code blocks are fenced, all links are relative or point to real URLs
- [x] T01X Validate SC-001: ask a developer unfamiliar with the project to read the README and explain back the core value proposition — confirm they understand human-owned session naming
- [x] T01X Validate SC-002: follow the quick-start section on a clean macOS machine and confirm first `cs` session completes in ≤5 minutes
- [x] T01X Validate SC-004: view `README.md` on GitHub in both light and dark modes and confirm the logo/header is legible in both
- [x] T01X Validate SC-003: confirm the usage reference covers all primary commands so no `--help` or source reading is needed for basic usage

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — can start immediately
- **Foundational (Phase 2)**: Depends on Phase 1 — BLOCKS all user story authoring
- **User Stories (Phases 3–5)**: All depend on Phase 2; can proceed in any order or in parallel since they touch distinct README sections
- **Polish (Phase 6)**: Depends on Phases 3–5 all complete; T012 and T013 can run in parallel

### User Story Dependencies

- **US1 (P1)**: Can start after Phase 2 — no dependency on US2 or US3
- **US2 (P2)**: Can start after Phase 2 — no dependency on US1 or US3
- **US3 (P3)**: Can start after Phase 2 — no dependency on US1 or US2

### Within Each User Story

- Tasks within US2 (T007 → T008 → T009) are sequential — prerequisites must appear before install steps
- Tasks within US3 (T010 → T011) are sequential — concept prose before examples
- US1 tasks (T005, T006) are sequential — value prop before demo block

---

## Parallel Opportunities

```
# Phase 1 — run in parallel:
T001  Create README.md skeleton
T002  Create assets/ directory

# Phase 3–5 — once Phase 2 completes, all three stories can start in parallel:
Phase 3 (T005–T006)   Hero section
Phase 4 (T007–T009)   Quick-start section
Phase 5 (T010–T011)   Concept section

# Phase 6 — polish tasks can start in parallel once all stories complete:
T012  Usage table
T013  Contributing paragraph
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup (T001–T002)
2. Complete Phase 2: Foundational (T003–T004) — logo + badges
3. Complete Phase 3: US1 (T005–T006) — value prop + demo block
4. **STOP and VALIDATE**: The README hero section should communicate the core identity
5. Proceed to US2 and US3

### Incremental Delivery

1. Setup + Foundational → README header exists
2. US1 → Hero section complete — README is "above the fold" useful
3. US2 → Quick-start complete — README is install-ready
4. US3 → Mental model section complete — README is concept-complete
5. Polish → README is launch-ready

---

## Notes

- All tasks write to `README.md` — sequential execution within a story, parallel between stories (different sections)
- No tests are generated (docs-only feature; TDD N/A per plan.md constitution check)
- Validation tasks (T016–T019) are manual checks against spec success criteria
- `assets/logo.svg` (T002 directory) is optional — only needed if an SVG is produced beyond the ASCII art
- Commit after each phase checkpoint to maintain a clean git history
